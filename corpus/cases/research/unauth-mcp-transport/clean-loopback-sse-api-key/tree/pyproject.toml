[project]
name = "analytics-mcp"
version = "0.9.0"
description = "Read-only analytics rollup tools over MCP/SSE"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.0",
  "mcp==1.9.0",
  "starlette==0.38.5",
  "uvicorn==0.30.6",
]

[build-system]
requires = ["hatchling==1.25.0"]
build-backend = "hatchling.build"
