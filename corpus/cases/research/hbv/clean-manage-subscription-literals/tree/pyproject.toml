[project]
name = "billing-mcp"
version = "1.5.2"
description = "Subscription state-transition tools for the billing team"
requires-python = ">=3.11"
dependencies = [
  "fastmcp==2.3.4",
  "httpx==0.27.2",
]

[build-system]
requires = ["hatchling==1.25.0"]
build-backend = "hatchling.build"
