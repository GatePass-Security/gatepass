[project]
name = "billing-support-agent"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "langchain-core>=0.3.0",
    "pydantic>=2.7",
]

[tool.setuptools.packages.find]
include = ["agent*"]
